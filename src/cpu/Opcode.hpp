#ifndef OPCODE_HPP
#define OPCODE_HPP

enum Opcode {
    ADD = 0,
    SUB = 1,
    LOAD = 2,
    STORE = 3,
    AND = 4,
    OR = 5, 
    MULT = 6,
    DIV = 7,
    IF_igual = 8,
    IF_maior = 9,
    ENQ = 10,
};

#endif